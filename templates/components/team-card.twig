<div class="team-card">
    <img src="images/{{ team.logo }}" alt="{{ team.name }} logo">
    <h2>{{ team.name }}</h2>
    <p>{{ team.description }}</p>
    <button class="discover-btn">Découvrez {{ team.name }}</button>
</div>
